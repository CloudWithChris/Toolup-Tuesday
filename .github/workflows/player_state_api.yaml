on:
  push: 
    branches:
      - "main"
  workflow_dispatch:

jobs:
  build_player_state_api:
    runs-on: ubuntu-latest
    steps:
      - name: "Build dotnet"
        run: dotnet build
        working-directory: src/SpaceBar.PlayerState/